вкл прелюдия;

про вернуть_массив(): массив(10, цел) нч
    пер результат: массив(10, цел);
    результат(0) := 69;
    вернуть результат;
кц

про главная() нч
    // пер результат: массив(10, цел) = вернуть_массив();
    печать(вернуть_массив()(0), «\н»);
кц

