вкл прелюдия;

конст ЛИМИТ := 1000;

про главная() нч
    // // СДЕЛАТЬ: определение нескольких переменных на одной строке.
    // // Хотя, с учётом инициализации, я не уверен, что мне нравится эта идея.
    // пер а, б, в: нат;
    пер а: цел = 0;
    пер б: цел = 1;
    пока а -? ЛИМИТ нч
        печать(а, «\н»);
        пер в: цел = а + б;
        а := б;
        б := в;
    кц
кц
